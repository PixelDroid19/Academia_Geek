/*Ejercicio 1
Crear un arreglo (temática libre), el cual sea recorrido con la función map()
 e imprimir por consola cada uno de sus elementos.*/
 let Vector = [1, 2, 3, 4, 5];
 
let recorrerVector = Vector.map(function(valor){
    console.log(valor);
});
 